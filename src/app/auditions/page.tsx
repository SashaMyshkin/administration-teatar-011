import { AuditionProps } from "@/types/audition";
import AuditionsUI from "./AuditionsUI";
import db from "@/db";
import audition from "@/db/schemas/audition";

export default async function Auditions() {
 // const result = db.select().from(audition);
  return <></>;
}
